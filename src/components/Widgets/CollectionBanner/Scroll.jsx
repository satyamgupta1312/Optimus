/**
 * Scroll.jsx — Collection Banner Scroll Mode (Carousel)
 *
 * Wiki: WIDGET-Collection-Banner.md §1 (Scroll Mode)
 *
 * Horizontal swipeable banner carousel.
 * Delegates entirely to BannerWithProductListing which handles:
 *   - Image display
 *   - Product resolution via catalog
 *   - PLP navigation on click
 */
export { default } from '../BannerWithProductListing';
